{% extends 'base.html.twig' %}

{% block body %}
    <div class="card-body mt-5">
        <img src="{{ detail.image }}">
        <div>{{ detail.Wording }}</div>
        <div>{{ detail.Ref }}</div>
        <div>{{ detail.Price }}</div>
        <div>{{ detail.Theme }}</div>
        <div>{{ detail.Rng }}</div>
        <div>{{ detail.Age }}</div>
        <div>{{ detail.Description }}</div>
    </div>
{% endblock %}